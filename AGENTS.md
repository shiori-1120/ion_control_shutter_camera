# Codex作業ループ（無限ループ運用）
このリポジトリでは、`docs/notes/redesign_plan_20260113.md`の方針に従って、停止指示があるまで反復的に改善を進める。

## 反復作業ループ
1. 現状確認: 対象ファイル/機能の最新状態を確認し、既存の設計・実装との差分を把握する。
2. 次タスク選定: `redesign_plan_20260113.md`のフェーズ/優先事項から次の一手を選ぶ。
3. 実装: 小さく安全な単位で変更する（1ファイル200?500行目安の分割を守る）。
4. 反映/記録: 変更点を関連ドキュメントに追記し、必要ならTODOを明文化する。
5. 検証: 可能な範囲でimport/py_compileなど安全な検証を実施する。
6. 報告: 進捗・次の手・未解決事項を簡潔に共有し、1に戻る。
7. 差分確認とコミット: 定期的に`git diff`で差分を確認し、適切な単位でコミットする。

※ ユーザーが「止める」「保留」「ここまで」等を指示した場合は即停止する。
y
## 設計方針（最優先）
- sweepはモジュール化し、dry/realで同じ処理フローを通す。
- 装置I/Oとデータ処理を分離し、dryで処理検証、realで取得検証ができるようにする。
- GUIはUIモジュールとコントローラに分離し、設定は上部タブに集約する。
- ログを強化し、カメラ接続のデバッグを容易にする。
- 実装内での無限ループは禁止し、安全停止を保証する。

## コーディング指針
- 安全性第一: ハードウェアに負荷をかける無限ループや過剰リクエストは厳禁。
- ファイル分割: 1ファイル200?500行目安、超える場合は分割を検討。
- 堅牢性: `print`ではなく`logging`を使用し、例外時は安全停止を優先。
- 可読性: 関数には型ヒントとDocstringを必ず付ける。
- 原因特定優先: 動作問題が出たら、修正案提示の前に原因を特定する。

## 検証の許可
以下の検証は許可を取らずに実行してよい:
- myenv で import と py_compile が通ること

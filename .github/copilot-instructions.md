以下の検証は許可を取らずに実行しても構いません。
検証: myenv で import と py_compile が通ること

shiori ではuvのmyenvを使っています。
tanakではC:/Users/tanak/miniforge3/Scripts/conda.exe run -n myenv --no-capture-output python -m compileall -q "c:/Users/tanak/デスクトップ/kariyama/single_ion_control/src"のように実行ができます